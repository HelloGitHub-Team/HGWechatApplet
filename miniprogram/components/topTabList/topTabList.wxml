<scroll-view scroll-x="{{true}}" class="tab-list">
  <view class="swiper-item {{item.selected?'choose-item':''}}" wx:for="{{tabList}}" data-index="{{index}}" bindtap="chooseItem">{{item.name}}</view>
  <slot></slot>
</scroll-view>